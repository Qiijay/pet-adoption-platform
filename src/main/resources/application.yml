server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pet_adoption?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf-8&allowPublicKeyRetrieval=true
    username: root
    password: DLAM0106

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.petadoption.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: pet-adoption-secret-key-2024-javaee-project
  expiration: 86400000

knife4j:
  enable: true
  openapi:
    title: 宠物领养平台API文档
    version: 1.0.0